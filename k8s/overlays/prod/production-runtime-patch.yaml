apiVersion: apps/v1
kind: Deployment
metadata:
  name: database-continuity
spec:
  template:
    spec:
      containers:
        - name: database-continuity
          env:
            - name: VAULT_TOKEN
              $patch: delete
