apiVersion: apps/v1
kind: Deployment
metadata:
  name: identity-service
spec:
  template:
    spec:
      containers:
        - name: identity-service
          env:
            - name: Identity__BootstrapAdmin__Password
              valueFrom:
                secretKeyRef:
                  name: identity-bootstrap-admin
                  key: password
