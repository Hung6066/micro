apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: spire-postgres
  namespace: spire
spec:
  plugins:
    - name: barman-cloud.cloudnative-pg.io
      isWALArchiver: true
      parameters:
        barmanObjectName: spire-postgres-azure-store
