apiVersion: v1
kind: ServiceAccount
metadata:
  name: dashboard-bff
  labels:
    app.kubernetes.io/name: dashboard-bff
    app.kubernetes.io/component: bff
automountServiceAccountToken: false
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: dashboard-bff
  labels:
    app.kubernetes.io/name: dashboard-bff
    app.kubernetes.io/component: bff
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: dashboard-bff
  template:
    metadata:
      labels:
        app.kubernetes.io/name: dashboard-bff
        app.kubernetes.io/component: bff
        app: dashboard-bff
    spec:
      serviceAccountName: dashboard-bff
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 1654
        runAsGroup: 1654
        fsGroup: 1654
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: dashboard-bff
          image: his-hope/dashboard-bff:latest
          imagePullPolicy: Always
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            readOnlyRootFilesystem: true
            seccompProfile:
              type: RuntimeDefault
            runAsUser: 1654
            runAsGroup: 1654
          ports:
            - name: http
              containerPort: 5600
              protocol: TCP
          env:
            - name: ConnectionStrings__Redis
              valueFrom:
                secretKeyRef:
                  name: dashboard-bff-secrets
                  key: redis-connection
            - name: Services__Patient
              value: "http://patientservice:5006"
            - name: Services__Clinical
              value: "http://clinicalservice:5005"
            - name: Services__Lab
              value: "http://labservice:5010"
            - name: Services__Billing
              value: "http://billingservice:5020"
            - name: Services__Pharmacy
              value: "http://pharmacyservice:5030"
            - name: Services__Appointment
              value: "http://appointmentservice:5007"
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 256Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 5600
            initialDelaySeconds: 15
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health/ready
              port: 5600
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 2
---
apiVersion: v1
kind: Service
metadata:
  name: dashboard-bff
  labels:
    app.kubernetes.io/name: dashboard-bff
    app.kubernetes.io/component: bff
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: dashboard-bff
  ports:
    - name: http
      port: 5600
      targetPort: 5600
      protocol: TCP
