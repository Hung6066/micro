apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: retry-policy
spec:
  syncPolicy:
    retry:
      limit: 3
      backoff:
        duration: 30s
        factor: 2
        maxDuration: 5m
