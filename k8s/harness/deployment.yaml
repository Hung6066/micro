apiVersion: apps/v1
kind: Deployment
metadata:
  name: agent-harness
  namespace: hishope
  labels:
    app.kubernetes.io/name: agent-harness
    app.kubernetes.io/component: platform
    app.kubernetes.io/part-of: his-hope
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: agent-harness
  template:
    metadata:
      labels:
        app.kubernetes.io/name: agent-harness
        app.kubernetes.io/component: platform
        app.kubernetes.io/part-of: his-hope
        app: agent-harness
    spec:
      serviceAccountName: agent-harness
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 1654
        runAsGroup: 1654
        fsGroup: 1654
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: agent-harness
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            readOnlyRootFilesystem: true
            seccompProfile:
              type: RuntimeDefault
            runAsUser: 1654
            runAsGroup: 1654
          image: his-hope-agentharness:latest
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 5200
              protocol: TCP
          envFrom:
            - configMapRef:
                name: agent-harness-config
          env:
            - name: AgentHarness__DatabaseConnectionString
              valueFrom:
                secretKeyRef:
                  name: harness-db-credentials
                  key: connection-string
            - name: AgentHarness__RabbitMQConnectionString
              valueFrom:
                secretKeyRef:
                  name: harness-rabbitmq-credentials
                  key: connection-string
            - name: AGENT_HARNESS_API_KEY
              valueFrom:
                secretKeyRef:
                  name: harness-api-key
                  key: api-key
            - name: ASPNETCORE_URLS
              value: "http://0.0.0.0:5200"
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: "http://otel-collector.hishop.svc.cluster.local:4317"
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /health
              port: 5200
            initialDelaySeconds: 30
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 5200
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 2
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: agent-harness
  namespace: hishope
  labels:
    app.kubernetes.io/name: agent-harness
    app.kubernetes.io/component: platform
  automountServiceAccountToken: false
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: agent-harness-pdb
  namespace: hishope
  labels:
    app.kubernetes.io/name: agent-harness
spec:
  maxUnavailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: agent-harness
