{
  "name": "his-hope-e2e",
  "version": "1.0.0",
  "description": "End-to-end tests for His.Hope EMR frontend using Cypress",
  "private": true,
  "scripts": {
    "cypress:open": "cypress open",
    "cypress:run": "cypress run",
    "cypress:run:chrome": "cypress run --browser chrome",
    "cypress:run:headless": "cypress run --headless",
    "e2e:ci": "cypress run --headless --browser chrome --record"
  },
  "devDependencies": {
    "cypress": "^13.8.0",
    "typescript": "^5.4.0"
  }
}
