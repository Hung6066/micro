# Local Compose development policy. Production must use a restricted subnet,
# TLS and a dedicated replication role managed by the secret manager.
local   all             all                             trust
host    all             all             172.16.0.0/12   scram-sha-256
host    replication     postgres        172.16.0.0/12   scram-sha-256
