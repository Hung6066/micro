import { ChangeDetectionStrategy, Component, input } from '@angular/core';
import { HisHopeTranslatePipe } from '@his-hope/frontend-foundation/i18n';

@Component({ selector: 'hh-diff-viewer', standalone: true, imports: [HisHopeTranslatePipe], changeDetection: ChangeDetectionStrategy.OnPush, template: `<div role="region" [attr.aria-label]="label() | hhTranslate"><pre>@for (line of lines(); track $index) { <span [class]="'hh-diff--' + line.kind">{{ line.text }}</span>\n }</pre></div>`, styles: [`:host { display: block; } pre { overflow: auto; margin: 0; padding: var(--size-timeline-dot); border: 1px solid var(--border-default); background: var(--surface-muted); font: var(--font-size-label)/var(--leading-body) var(--font-mono); } span { display: block; white-space: pre-wrap; }.hh-diff--added { color: var(--color-success); }.hh-diff--removed { color: var(--color-danger); }.hh-diff--context { color: var(--text-secondary); }`] })
export class HisHopeDiffViewerComponent { readonly label = input('domain.changes'); readonly lines = input<readonly { text: string; kind: 'added' | 'removed' | 'context' }[]>([]); }
