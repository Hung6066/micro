import { ChangeDetectionStrategy, Component, input } from '@angular/core';

@Component({ selector: 'hh-diff-viewer', standalone: true, changeDetection: ChangeDetectionStrategy.OnPush, template: `<div role="region" [attr.aria-label]="label()"><pre>@for (line of lines(); track $index) { <span [class]="'hh-diff--' + line.kind">{{ line.text }}</span>\n }</pre></div>`, styles: [`:host { display: block; } pre { overflow: auto; margin: 0; padding: 14px; border: 1px solid var(--border-default); background: var(--surface-muted); font: 13px/1.6 var(--font-family-mono, monospace); } span { display: block; white-space: pre-wrap; }.hh-diff--added { color: var(--color-success); }.hh-diff--removed { color: var(--color-danger); }.hh-diff--context { color: var(--text-secondary); }`] })
export class HisHopeDiffViewerComponent { readonly label = input('Changes'); readonly lines = input<readonly { text: string; kind: 'added' | 'removed' | 'context' }[]>([]); }