## 0.3.0

- Add `@his-hope/mobile-foundation/angular` entry point with reusable shell:
  guards, session interceptor, lock service, offline sync queue, paged list
  controller, resource table/state controllers, table bulk/export API service,
  schema-form mapper, and platform capability DI tokens.
- Export `HisHopeOfflineSyncConfig` and `HisHopeMobileAuthActions` contracts.

## 0.2.0

- Export `RuntimeConfigService` from the package entry point for app adapters.
- Align mobile runtime resolution with the admin-app `RuntimeConfigService` pattern.

## 0.1.0

- Added mobile runtime, secure storage, biometric, push and deep-link contracts.
