interface:
  display_name: "Micro"
  short_description: "Repo-specific patterns and workflows for micro"
  default_prompt: "Use the micro repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true